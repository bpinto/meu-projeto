pt-BR:
  errors:
    messages:
      invalid_date: "não é uma data válida"
      invalid_time: "não é um horário válido"
      invalid_datetime: "não é uma data válida"
      is_at: "deve ser igual a %{restriction}"
      before: "deve ser antes de %{restriction}"
      on_or_before: "deve ser igual ou anterior a %{restriction}"
      after: "deve ser posterior a %{restriction}"
      on_or_after: "deve ser igual ou posterior a %{restriction}"
  validates_timeliness:
    error_value_formats:
      date: '%d-%m-%Y'
      time: '%H:%M:%S'
      datetime: '%d-%m-%Y %H:%M:%S'
